## Packages
canvas-confetti | For a delightful success animation after checkout
@types/canvas-confetti | Types for canvas-confetti

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

Images are imported statically from @assets/.
Form uses React Hook Form with Zod validation based on the shared schema.
